---
- name: Delete ZFS Pools
  shell: |
    incus storage delete {{ item.name }}
  register: delete_zfs_pools
  ignore_errors: true
  with_items:
    - "{{ pools.zfs }}"
- debug: msg={{ delete_zfs_pools }}

