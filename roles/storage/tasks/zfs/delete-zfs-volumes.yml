---
- name: Delete ZFS Volumes
  shell: |
    incus storage volume delete {{ item.pool }} {{ item.name }}
  register: delete_zfs_volumes
  ignore_errors: true
  with_items:
    - "{{ volumes.zfs }}"
- debug: msg={{ delete_zfs_volumes }}

